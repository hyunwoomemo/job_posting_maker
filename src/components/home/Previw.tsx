import styled from "@emotion/styled";
import React from "react";

const Previw = () => {
  return (
    <Base>
      <div></div>
    </Base>
  );
};

const Base = styled.div`
  flex: 4 4 auto;
`;

export default Previw;
