import styled from "@emotion/styled";
import React from "react";

const Header = () => {
  return <Base>3분안에 채용 공고 만들기</Base>;
};

const Base = styled.div`
  padding: 2rem 0;
  font-size: 24px;
`;

export default Header;
